# base

The default pack: the universal AI-first engineering workflow, independent of stack. Artifact prefix `dev-`.

Contents and the three-gate owner flow are documented in the [repository README](../../README.md). Configure it per project with [`templates/dev-project-profile.md`](templates/dev-project-profile.md).
